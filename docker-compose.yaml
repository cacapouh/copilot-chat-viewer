services:
  viewer:
    build: .
    image: copilot-chat-viewer:dev
    container_name: copilot-chat-viewer
    ports:
      - "5173:5173"
    volumes:
      # ホスト側で編集 → コンテナの Vite が即反映するため bind mount。
      # node_modules はコンテナ側のものを使うので named volume で隠す。
      - ./:/app
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: ["npm", "run", "dev"]
    stdin_open: true
    tty: true

volumes:
  node_modules:
